# Self-Bot Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在阿里云服务器上部署个人 bot——飞书私人模式（RAG 全记忆 + 三种子模式）和网页公开模式（仅公开信息 + SSE 流式输出）。

**Architecture:** 共享 RAG 模块（ChromaDB + 千问 text-embedding-v3）供私人 bot 检索历史片段；MiniMax-M2.7 生成回复；私人 bot 通过飞书长连接运行，公开 bot 通过 Flask + SSE 运行，nginx 反代 443 端口。

**Tech Stack:** Python 3.11, lark-oapi, Flask, chromadb, openai SDK（接千问和 MiniMax），BeautifulSoup4，python-dotenv，nginx，systemd

---

## File Map

| 文件 | 职责 |
|------|------|
| `/home/bots/ingest.py` | 解析三个数据源，embedding，写入 ChromaDB |
| `/home/bots/rag.py` | 封装向量检索，供两个 bot 调用 |
| `/home/bots/self_feishu.py` | 飞书长连接私人 bot |
| `/home/bots/self_web.py` | Flask 公开 bot HTTP API（SSE） |
| `/home/bots/data/memories.json` | Claude 记忆文件（私人 bot system prompt 常驻） |
| `/home/bots/data/public_bio.md` | 手动整理公开简介（公开 bot system prompt） |
| `/home/bots/data/正觉的笔记.html` | Flomo 笔记导出 |
| `/home/bots/data/conversations.json` | Claude 对话导出 |
| `/home/bots/data/chat-memo_49.txt` | Gemini 对话导出 |
| `/home/bots/chroma_db/` | ChromaDB 持久化数据（ingest.py 创建） |
| `/home/bots/.env` | 所有密钥和配置 |
| `/home/bots/requirements.txt` | Python 依赖 |
| `chat-widget.html` | 嵌入个人主页的对话 widget（本地开发） |
| `/etc/systemd/system/self-feishu.service` | 私人 bot 进程管理 |
| `/etc/systemd/system/self-web.service` | 公开 bot 进程管理 |
| `/etc/nginx/conf.d/self-bot.conf` | nginx 反向代理配置 |

---

## Task 1: 服务器环境初始化

**Files:**
- Create: `/home/bots/requirements.txt`
- Create: `/home/bots/.env`

- [ ] **Step 1: SSH 登录服务器，创建目录结构**

```bash
ssh root@47.108.56.88
mkdir -p /home/bots/data /home/bots/chroma_db
```

- [ ] **Step 2: 安装 Python 依赖**

创建 `/home/bots/requirements.txt`：

```
lark-oapi
openai
flask
chromadb
beautifulsoup4
python-dotenv
```

```bash
cd /home/bots
pip3 install -r requirements.txt
```

预期：所有包安装成功，`chromadb` 版本 >= 0.4。

- [ ] **Step 3: 写 .env 文件**

创建 `/home/bots/.env`：

```dotenv
# 飞书私人 bot（新建一个飞书应用）
SELF_FEISHU_APP_ID=
SELF_FEISHU_APP_SECRET=

# MiniMax（生成）
MINIMAX_API_KEY=sk-api-HZ8C0Ly8rG0TMs8Zi5l71Gu44C6B2080YnrnpmP4EPHS8HbLoCpqP4ighzqbvK16STzDEcrILciZP1os4osy0W3VZLTFk-BaD1V-NVzzMMEF9xRcxTZ9l1U

# 千问（embedding）
QIANWEN_API_KEY=

# 路径
CHROMA_PATH=/home/bots/chroma_db
DATA_PATH=/home/bots/data
```

- [ ] **Step 4: 上传数据文件**

在本地执行（替换 `/path/to/` 为实际路径）：

```bash
scp /path/to/memories.json root@47.108.56.88:/home/bots/data/
scp /path/to/正觉的笔记.html root@47.108.56.88:/home/bots/data/
scp /path/to/conversations.json root@47.108.56.88:/home/bots/data/
scp /path/to/chat-memo_49.txt root@47.108.56.88:/home/bots/data/
```

- [ ] **Step 5: 创建 public_bio.md（手动填写）**

创建 `/home/bots/data/public_bio.md`，内容是对外展示的简介，示例：

```markdown
我叫正觉，做过互联网产品、AI 大模型方向的产品工作，现在在一家有非洲出海业务的公司。

## 主要项目
- ...

## 核心观点
- ...
```

这个文件由你自己填写，不需要代码处理。

- [ ] **Step 6: 验证目录结构**

```bash
ls -la /home/bots/data/
```

预期：看到 `memories.json`、`正觉的笔记.html`、`conversations.json`、`chat-memo_49.txt`、`public_bio.md`。

---

## Task 2: 数据入库脚本（ingest.py）

**Files:**
- Create: `/home/bots/ingest.py`

`memories.json` 常驻 system prompt，不入库。入库三个来源：Flomo 笔记、Claude 对话、Gemini 对话，全部标记 `private=True`。

- [ ] **Step 1: 写 ingest.py**

创建 `/home/bots/ingest.py`：

```python
import os, json, re, hashlib
from bs4 import BeautifulSoup
from openai import OpenAI
import chromadb
from dotenv import load_dotenv

load_dotenv()

CHROMA_PATH = os.environ["CHROMA_PATH"]
DATA_PATH   = os.environ["DATA_PATH"]

qw = OpenAI(
    api_key=os.environ["QIANWEN_API_KEY"],
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)

chroma = chromadb.PersistentClient(path=CHROMA_PATH)
col    = chroma.get_or_create_collection(
    "memories",
    metadata={"hnsw:space": "cosine"},
)


def embed(text: str) -> list[float]:
    resp = qw.embeddings.create(
        model="text-embedding-v3",
        input=text[:2000],
    )
    return resp.data[0].embedding


def chunk_id(source: str, idx: int) -> str:
    return hashlib.md5(f"{source}:{idx}".encode()).hexdigest()


def add_chunk(source: str, idx: int, text: str):
    text = text.strip()
    if not text or len(text) < 10:
        return
    cid = chunk_id(source, idx)
    # 跳过已存在的 chunk（断点续传）
    existing = col.get(ids=[cid])
    if existing["ids"]:
        return
    vec = embed(text)
    col.add(
        ids=[cid],
        embeddings=[vec],
        documents=[text],
        metadatas=[{"source": source, "private": True}],
    )
    print(f"  [{source}] #{idx} added ({len(text)} chars)")


# ── 1. Flomo 笔记 ──────────────────────────────────────────────────────
def ingest_flomo():
    path = os.path.join(DATA_PATH, "正觉的笔记.html")
    with open(path, encoding="utf-8") as f:
        soup = BeautifulSoup(f, "html.parser")

    memos = soup.select("li") or soup.select(".memo") or soup.find_all("div", class_=re.compile("memo|note"))
    if not memos:
        # fallback：提取所有 <p> 文本
        memos = soup.find_all("p")

    print(f"Flomo: found {len(memos)} items")
    for i, m in enumerate(memos):
        text = m.get_text(separator=" ", strip=True)
        add_chunk("flomo", i, text)


# ── 2. Claude 对话 ─────────────────────────────────────────────────────
def ingest_claude_conversations():
    path = os.path.join(DATA_PATH, "conversations.json")
    with open(path, encoding="utf-8") as f:
        data = json.load(f)

    # Claude 导出格式：列表，每项有 chat_messages
    idx = 0
    for conv in data:
        messages = conv.get("chat_messages") or conv.get("conversation") or []
        pairs = []
        for msg in messages:
            role = msg.get("sender") or msg.get("role", "")
            text = msg.get("text") or msg.get("content", "")
            if isinstance(text, list):  # content 可能是列表
                text = " ".join(t.get("text", "") for t in text if isinstance(t, dict))
            if role in ("human", "user") and text.strip():
                pairs.append(("Q", text.strip()))
            elif role in ("assistant",) and text.strip():
                pairs.append(("A", text.strip()))

        # 每两条 Q+A 合并为一个 chunk
        for j in range(0, len(pairs) - 1, 2):
            chunk = f"{pairs[j][1]}\n\n{pairs[j+1][1]}" if j+1 < len(pairs) else pairs[j][1]
            add_chunk("claude", idx, chunk)
            idx += 1

    print(f"Claude: ingested {idx} Q&A pairs")


# ── 3. Gemini 对话 ─────────────────────────────────────────────────────
def ingest_gemini():
    path = os.path.join(DATA_PATH, "chat-memo_49.txt")
    with open(path, encoding="utf-8") as f:
        raw = f.read()

    # 按空行分段，每段一个 chunk
    segments = [s.strip() for s in re.split(r"\n{2,}", raw) if s.strip()]
    print(f"Gemini: found {len(segments)} segments")
    for i, seg in enumerate(segments):
        add_chunk("gemini", i, seg)


# ── 主入口 ─────────────────────────────────────────────────────────────
if __name__ == "__main__":
    print("=== 开始数据入库 ===")
    ingest_flomo()
    ingest_claude_conversations()
    ingest_gemini()
    total = col.count()
    print(f"\n=== 完成！ChromaDB 共 {total} 条记录 ===")
```

- [ ] **Step 2: 运行入库脚本**

```bash
cd /home/bots
python3 ingest.py
```

预期输出示例：
```
=== 开始数据入库 ===
Flomo: found 342 items
  [flomo] #0 added (87 chars)
  ...
Claude: ingested 156 Q&A pairs
Gemini: found 89 segments
=== 完成！ChromaDB 共 587 条记录 ===
```

如果 Flomo 解析到 0 条，打印 HTML 结构调试：`python3 -c "from bs4 import BeautifulSoup; soup=BeautifulSoup(open('/home/bots/data/正觉的笔记.html').read(),'html.parser'); print(soup.prettify()[:2000])"`，根据实际标签调整 `ingest_flomo()` 中的选择器。

- [ ] **Step 3: 验证入库结果**

```bash
python3 - <<'EOF'
import chromadb, os
from dotenv import load_dotenv
load_dotenv()
c = chromadb.PersistentClient(path=os.environ["CHROMA_PATH"])
col = c.get_collection("memories")
print("Total:", col.count())
sample = col.peek(5)
for doc in sample["documents"]:
    print("---", doc[:100])
EOF
```

预期：打印总数和 5 条样本文本。

- [ ] **Step 4: Commit**

```bash
cd /home/bots
git init  # 如果还没有 git
git add ingest.py requirements.txt .env
git commit -m "feat: data ingestion script with ChromaDB"
```

---

## Task 3: RAG 检索模块（rag.py）

**Files:**
- Create: `/home/bots/rag.py`

- [ ] **Step 1: 写 rag.py**

创建 `/home/bots/rag.py`：

```python
import os
from openai import OpenAI
import chromadb
from dotenv import load_dotenv

load_dotenv()

_qw = OpenAI(
    api_key=os.environ["QIANWEN_API_KEY"],
    base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
)
_chroma = chromadb.PersistentClient(path=os.environ["CHROMA_PATH"])
_col    = _chroma.get_collection("memories")


def _embed(text: str) -> list[float]:
    resp = _qw.embeddings.create(
        model="text-embedding-v3",
        input=text[:2000],
    )
    return resp.data[0].embedding


def retrieve(query: str, n: int = 5) -> list[str]:
    """
    检索与 query 最相关的历史片段，返回文本列表。
    全部内容均为私人内容，仅私人 bot 调用。
    """
    vec = _embed(query)
    results = _col.query(
        query_embeddings=[vec],
        n_results=n,
    )
    docs = results["documents"][0] if results["documents"] else []
    return docs
```

- [ ] **Step 2: 手动测试检索**

```bash
python3 - <<'EOF'
from rag import retrieve
hits = retrieve("我对工作的看法")
for i, h in enumerate(hits):
    print(f"\n[{i+1}]", h[:150])
EOF
```

预期：打印 5 条相关历史片段，内容与查询语义相关。

- [ ] **Step 3: Commit**

```bash
git add rag.py
git commit -m "feat: RAG retrieval module"
```

---

## Task 4: 私人飞书 bot（self_feishu.py）

**Files:**
- Create: `/home/bots/self_feishu.py`

前提：在飞书开放平台新建一个应用，开启「机器人」能力，订阅「接收消息」事件，并在「事件与回调」页面选择「长连接」模式，将 App ID / App Secret 填入 `.env`。

- [ ] **Step 1: 读取 memories.json 内容**

在实现 bot 前，先确认 memories.json 的格式：

```bash
python3 - <<'EOF'
import json
data = json.load(open("/home/bots/data/memories.json"))
print(type(data))
if isinstance(data, list):
    print("List, first item:", str(data[0])[:200])
elif isinstance(data, dict):
    print("Dict, keys:", list(data.keys())[:5])
EOF
```

根据实际格式，`load_memories()` 函数提取纯文本。下方代码假设格式为列表，每项有 `content` 字段；如果格式不同，调整 `load_memories()`。

- [ ] **Step 2: 写 self_feishu.py**

创建 `/home/bots/self_feishu.py`：

```python
import os, json, time, re
import lark_oapi as lark
from lark_oapi.api.im.v1 import *
from openai import OpenAI
from dotenv import load_dotenv
from rag import retrieve

load_dotenv()

APP_ID     = os.environ["SELF_FEISHU_APP_ID"]
APP_SECRET = os.environ["SELF_FEISHU_APP_SECRET"]
MODEL      = "MiniMax-M2.7"

ai = OpenAI(
    api_key=os.environ["MINIMAX_API_KEY"],
    base_url="https://api.minimaxi.com/v1",
)
feishu = lark.Client.builder().app_id(APP_ID).app_secret(APP_SECRET).build()

conversations: dict[str, list] = {}
processed: set[str] = set()


def load_memories() -> str:
    path = os.path.join(os.environ["DATA_PATH"], "memories.json")
    try:
        data = json.load(open(path, encoding="utf-8"))
        if isinstance(data, list):
            return "\n".join(
                item.get("content", str(item)) for item in data
            )
        return json.dumps(data, ensure_ascii=False, indent=2)
    except Exception:
        return ""


MEMORIES = load_memories()

SYSTEM = f"""你是我自己的第二自我。你完整了解我的经历、想法和决策历史。

## 我的记忆库
{MEMORIES}

## 三种子模式（根据输入自然切换，不做显式提示）

**推演**：当我描述决策/纠结时——用我自己的思维框架拆解，给判断，也说出反面。
**镜子**：当我描述感受/陈述一件事时——先反问，帮我把想法说清楚。
**记忆**：当我问"我之前怎么看……"时——从下方检索到的历史片段中引用原文。

## 行为约束
- 不表演聪明，不给标准答案
- 镜子优先：先确认我在说什么，再给意见
- 有原文时引用原文，不重新诠释
- 可以直接挑战，但要说出理由
- 回复简洁，不写长段落"""


def is_memory_query(text: str) -> bool:
    keywords = ["之前", "曾经", "以前", "当时", "历史", "记得", "怎么看", "说过", "想过"]
    return any(k in text for k in keywords)


def ask_self(user_id: str, text: str) -> str:
    hist = conversations.setdefault(user_id, [])

    # 如果是记忆查询，先检索相关片段
    rag_context = ""
    if is_memory_query(text):
        hits = retrieve(text, n=5)
        if hits:
            rag_context = "\n\n## 检索到的相关历史片段\n" + "\n---\n".join(hits)

    hist.append({"role": "user", "content": text})
    if len(hist) > 20:
        hist[:] = hist[-20:]

    system_with_rag = SYSTEM + rag_context

    resp = ai.chat.completions.create(
        model=MODEL,
        max_tokens=512,
        messages=[{"role": "system", "content": system_with_rag}] + hist,
    )
    raw = resp.choices[0].message.content or ""
    answer = re.sub(r"<think>.*?</think>", "", raw, flags=re.DOTALL).strip()
    hist.append({"role": "assistant", "content": answer})
    return answer


def _send_one(chat_id: str, text: str):
    req = CreateMessageRequest.builder() \
        .receive_id_type("chat_id") \
        .request_body(
            CreateMessageRequestBody.builder()
            .receive_id(chat_id)
            .msg_type("text")
            .content(json.dumps({"text": text}))
            .build()
        ).build()
    feishu.im.v1.message.create(req)


def send_reply(chat_id: str, text: str):
    lines = [l.strip() for l in text.split("\n") if l.strip()]
    for i, line in enumerate(lines):
        _send_one(chat_id, line)
        if i < len(lines) - 1:
            time.sleep(0.4)


def on_message(data: P2ImMessageReceiveV1):
    msg    = data.event.message
    sender = data.event.sender

    if msg.message_type != "text":
        return
    if sender.sender_type == "bot":
        return
    if msg.message_id in processed:
        return
    processed.add(msg.message_id)

    text    = json.loads(msg.content).get("text", "").strip()
    user_id = sender.sender_id.open_id
    chat_id = msg.chat_id

    if not text:
        return

    try:
        answer = ask_self(user_id, text)
        send_reply(chat_id, answer)
    except Exception as e:
        send_reply(chat_id, f"出了点问题：{e}")


handler = lark.EventDispatcherHandler.builder("", "") \
    .register_p2_im_message_receive_v1(on_message) \
    .build()

ws = lark.ws.Client(
    APP_ID, APP_SECRET,
    event_handler=handler,
    log_level=lark.LogLevel.INFO,
)

if __name__ == "__main__":
    print("Self Bot 启动（私人飞书长连接）")
    ws.start()
```

- [ ] **Step 3: 填写飞书应用信息到 .env 并本地测试**

先确保 `.env` 中 `SELF_FEISHU_APP_ID` 和 `SELF_FEISHU_APP_SECRET` 已填写，然后：

```bash
python3 self_feishu.py
```

预期：看到 `connected to wss://msg-frontier.feishu.cn`，向 bot 发消息，收到回复。

- [ ] **Step 4: Commit**

```bash
git add self_feishu.py
git commit -m "feat: private self bot with Feishu long connection and RAG"
```

---

## Task 5: 公开网页 bot（self_web.py）

**Files:**
- Create: `/home/bots/self_web.py`

- [ ] **Step 1: 写 self_web.py**

创建 `/home/bots/self_web.py`：

```python
import os, re, json
from flask import Flask, request, Response, stream_with_context
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()

MODEL = "MiniMax-M2.7"
ai = OpenAI(
    api_key=os.environ["MINIMAX_API_KEY"],
    base_url="https://api.minimaxi.com/v1",
)

app = Flask(__name__)
sessions: dict[str, list] = {}


def load_public_bio() -> str:
    path = os.path.join(os.environ["DATA_PATH"], "public_bio.md")
    try:
        return open(path, encoding="utf-8").read()
    except FileNotFoundError:
        return "（暂无公开简介）"


PUBLIC_BIO = load_public_bio()

SYSTEM = f"""你代表我（网站主人）和访问我个人主页的访客交流。

## 关于我
{PUBLIC_BIO}

## 行为规则
- 第一层：介绍我的经历、项目、核心观点
- 第二层：聊深了，用我自己的思维框架回应访客问题
- 只分享公开信息，不透露私人日记、私下想法、未发布内容
- 如果被问到私人信息，礼貌说"这部分我没有公开"
- 语气自然，像本人在聊天，不是客服"""


@app.route("/api/chat", methods=["POST"])
def chat():
    data    = request.get_json(force=True)
    text    = (data.get("message") or "").strip()
    session = data.get("session_id", "default")

    if not text:
        return {"error": "empty message"}, 400

    hist = sessions.setdefault(session, [])
    hist.append({"role": "user", "content": text})
    if len(hist) > 20:
        hist[:] = hist[-20:]

    def generate():
        buffer = ""
        full_text = []
        stream = ai.chat.completions.create(
            model=MODEL,
            max_tokens=512,
            stream=True,
            messages=[{"role": "system", "content": SYSTEM}] + hist,
        )
        in_think = False
        for chunk in stream:
            delta = chunk.choices[0].delta.content or ""
            buffer += delta

            # 过滤 <think>...</think>
            while True:
                if in_think:
                    end = buffer.find("</think>")
                    if end == -1:
                        buffer = ""
                        break
                    buffer = buffer[end + 8:]
                    in_think = False
                else:
                    start = buffer.find("<think>")
                    if start == -1:
                        full_text.append(buffer)
                        yield f"data: {json.dumps({'text': buffer})}\n\n"
                        buffer = ""
                        break
                    if start > 0:
                        full_text.append(buffer[:start])
                        yield f"data: {json.dumps({'text': buffer[:start]})}\n\n"
                    buffer = buffer[start + 7:]
                    in_think = True

        if buffer:
            full_text.append(buffer)
            yield f"data: {json.dumps({'text': buffer})}\n\n"

        # 保存完整回复到历史，支持正常多轮对话
        hist.append({"role": "assistant", "content": "".join(full_text)})
        yield "data: [DONE]\n\n"

    return Response(
        stream_with_context(generate()),
        mimetype="text/event-stream",
        headers={
            "Cache-Control": "no-cache",
            "X-Accel-Buffering": "no",
            "Access-Control-Allow-Origin": "*",
        },
    )


@app.route("/api/chat", methods=["OPTIONS"])
def chat_options():
    return "", 204, {
        "Access-Control-Allow-Origin": "*",
        "Access-Control-Allow-Headers": "Content-Type",
        "Access-Control-Allow-Methods": "POST, OPTIONS",
    }


if __name__ == "__main__":
    app.run(host="0.0.0.0", port=5001, debug=False)
```

- [ ] **Step 2: 本地测试 API**

```bash
python3 self_web.py &
curl -X POST http://localhost:5001/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好，介绍一下你自己", "session_id": "test"}' \
  --no-buffer
```

预期：看到 SSE 流式输出，`data: {"text": "..."}` 逐块返回，最后 `data: [DONE]`。

- [ ] **Step 3: Commit**

```bash
git add self_web.py
git commit -m "feat: public web bot with Flask SSE"
```

---

## Task 6: 网页 Chat Widget

**Files:**
- Create: `chat-widget.html`（本地开发，之后嵌入个人主页）

- [ ] **Step 1: 写 chat-widget.html**

在本地 `/Users/aero/Desktop/常用文件汇总/代码/skills/` 创建 `chat-widget.html`：

```html
<!DOCTYPE html>
<html lang="zh">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Chat</title>
<style>
  * { box-sizing: border-box; margin: 0; padding: 0; }

  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    background: #f5f5f5;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
  }

  .chat-container {
    width: 100%;
    max-width: 480px;
    height: 100vh;
    max-height: 700px;
    background: #fff;
    display: flex;
    flex-direction: column;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 4px 24px rgba(0,0,0,.12);
  }

  .chat-header {
    padding: 16px 20px;
    background: #1a1a1a;
    color: #fff;
    font-weight: 600;
    font-size: 15px;
  }

  .chat-messages {
    flex: 1;
    overflow-y: auto;
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .msg {
    max-width: 78%;
    padding: 10px 14px;
    border-radius: 18px;
    line-height: 1.5;
    font-size: 14px;
    word-break: break-word;
  }

  .msg.user {
    align-self: flex-end;
    background: #1a1a1a;
    color: #fff;
    border-bottom-right-radius: 4px;
  }

  .msg.bot {
    align-self: flex-start;
    background: #f0f0f0;
    color: #1a1a1a;
    border-bottom-left-radius: 4px;
  }

  .msg.bot.streaming::after {
    content: "▋";
    animation: blink .7s step-start infinite;
  }

  @keyframes blink { 50% { opacity: 0; } }

  .chat-input-area {
    display: flex;
    gap: 8px;
    padding: 12px 16px;
    border-top: 1px solid #eee;
    background: #fff;
  }

  .chat-input {
    flex: 1;
    padding: 10px 14px;
    border: 1px solid #ddd;
    border-radius: 22px;
    font-size: 14px;
    outline: none;
    resize: none;
    height: 42px;
    line-height: 22px;
  }

  .chat-input:focus { border-color: #1a1a1a; }

  .send-btn {
    width: 42px;
    height: 42px;
    border: none;
    border-radius: 50%;
    background: #1a1a1a;
    color: #fff;
    font-size: 18px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .send-btn:disabled { background: #ccc; cursor: default; }
</style>
</head>
<body>
<div class="chat-container">
  <div class="chat-header">和我聊聊</div>
  <div class="chat-messages" id="messages">
    <div class="msg bot">你好，我是网站主人。有什么想聊的？</div>
  </div>
  <div class="chat-input-area">
    <textarea
      class="chat-input"
      id="input"
      placeholder="发消息…"
      rows="1"
    ></textarea>
    <button class="send-btn" id="sendBtn">↑</button>
  </div>
</div>

<script>
  // 修改这里为你的服务器地址
  const API = "https://47.108.56.88/api/chat";
  const SESSION = "sess_" + Math.random().toString(36).slice(2);

  const messagesEl = document.getElementById("messages");
  const inputEl    = document.getElementById("input");
  const sendBtn    = document.getElementById("sendBtn");

  function appendMsg(role, text) {
    const el = document.createElement("div");
    el.className = `msg ${role}`;
    el.textContent = text;
    messagesEl.appendChild(el);
    messagesEl.scrollTop = messagesEl.scrollHeight;
    return el;
  }

  async function send() {
    const text = inputEl.value.trim();
    if (!text || sendBtn.disabled) return;

    inputEl.value = "";
    sendBtn.disabled = true;
    appendMsg("user", text);

    const botEl = appendMsg("bot", "");
    botEl.classList.add("streaming");

    try {
      const resp = await fetch(API, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ message: text, session_id: SESSION }),
      });

      const reader = resp.body.getReader();
      const decoder = new TextDecoder();
      let buf = "";

      while (true) {
        const { done, value } = await reader.read();
        if (done) break;
        buf += decoder.decode(value, { stream: true });

        const lines = buf.split("\n");
        buf = lines.pop();

        for (const line of lines) {
          if (!line.startsWith("data: ")) continue;
          const payload = line.slice(6).trim();
          if (payload === "[DONE]") break;
          try {
            const { text: chunk } = JSON.parse(payload);
            botEl.textContent += chunk;
            messagesEl.scrollTop = messagesEl.scrollHeight;
          } catch {}
        }
      }
    } catch (e) {
      botEl.textContent = "连接失败，请稍后再试。";
    } finally {
      botEl.classList.remove("streaming");
      sendBtn.disabled = false;
      inputEl.focus();
    }
  }

  sendBtn.addEventListener("click", send);
  inputEl.addEventListener("keydown", e => {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      send();
    }
  });
</script>
</body>
</html>
```

- [ ] **Step 2: 本地预览**

用浏览器打开 `chat-widget.html`，此时 API 地址指向服务器（Task 7 完成后才能真正测试）。可先将 `const API` 改为 `http://localhost:5001/api/chat` 配合本地运行的 `self_web.py` 测试。

- [ ] **Step 3: Commit**

```bash
git add chat-widget.html
git commit -m "feat: chat widget with SSE streaming"
```

---

## Task 7: 服务器部署（nginx + systemd）

**Files:**
- Create: `/etc/systemd/system/self-feishu.service`
- Create: `/etc/systemd/system/self-web.service`
- Create: `/etc/nginx/conf.d/self-bot.conf`

- [ ] **Step 1: 创建 systemd service——私人 bot**

在服务器上执行：

```bash
cat > /etc/systemd/system/self-feishu.service << 'EOF'
[Unit]
Description=Self Feishu Bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/bots
ExecStart=/usr/bin/python3 /home/bots/self_feishu.py
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF
```

- [ ] **Step 2: 创建 systemd service——公开 bot**

```bash
cat > /etc/systemd/system/self-web.service << 'EOF'
[Unit]
Description=Self Web Bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/bots
ExecStart=/usr/bin/python3 /home/bots/self_web.py
Restart=always
RestartSec=5
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
EOF
```

- [ ] **Step 3: 启动并设置开机自启**

```bash
systemctl daemon-reload
systemctl enable self-feishu self-web
systemctl start self-feishu self-web
systemctl status self-feishu self-web
```

预期：两个服务均显示 `active (running)`。

- [ ] **Step 4: 配置 nginx 反向代理**

前提：服务器已有 SSL 证书（如 Let's Encrypt）。将域名替换为你的实际域名。

```bash
cat > /etc/nginx/conf.d/self-bot.conf << 'EOF'
server {
    listen 443 ssl;
    server_name your-domain.com;  # 替换为你的域名

    ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    location /api/ {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;

        # SSE 必需配置
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 120s;
        chunked_transfer_encoding on;
    }
}
EOF
```

如果没有域名，暂时用 IP + HTTP 测试（将 `listen 443 ssl` 改为 `listen 80`，去掉 ssl 行）。

```bash
nginx -t && systemctl reload nginx
```

预期：`nginx -t` 输出 `syntax is ok`，`test is successful`。

- [ ] **Step 5: 端对端测试**

```bash
# 测试公开 bot API
curl -X POST https://your-domain.com/api/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "你好", "session_id": "e2e"}' \
  --no-buffer
```

预期：SSE 流式返回，包含 `data: {"text": "..."}` 多行和最终 `data: [DONE]`。

- [ ] **Step 6: 查看日志**

```bash
journalctl -u self-feishu -f   # 私人 bot 日志
journalctl -u self-web -f      # 公开 bot 日志
```

- [ ] **Step 7: 将 Ryven bot 也迁移到服务器（可选）**

```bash
# 上传 ryven bot
scp /Users/aero/Desktop/常用文件汇总/代码/skills/ryven-bot/bot.py root@47.108.56.88:/home/bots/ryven_bot.py

# 创建 systemd service
cat > /etc/systemd/system/ryven-bot.service << 'EOF'
[Unit]
Description=Ryven Feishu Bot
After=network.target

[Service]
Type=simple
WorkingDirectory=/home/bots
ExecStart=/usr/bin/python3 /home/bots/ryven_bot.py
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

systemctl enable ryven-bot
systemctl start ryven-bot
systemctl status ryven-bot
```

---

## 注意事项

1. **千问 API Key**：去 https://dashscope.aliyuncs.com 申请，填入 `.env` 的 `QIANWEN_API_KEY`。
2. **Flomo 解析**：如果 `ingest_flomo()` 找到 0 条，用调试命令查看 HTML 结构，调整选择器。
3. **memories.json 格式**：Task 4 Step 1 先检查格式，再运行 bot；如果格式不是 `[{"content": "..."}]`，调整 `load_memories()`。
4. **SSL 证书**：如果没有域名，可先用 HTTP 测试 widget；正式上线再配 Let's Encrypt。
5. **ingest.py 断点续传**：脚本已支持跳过已存在的 chunk，重新运行不会重复入库。
